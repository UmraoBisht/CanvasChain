# Canvas Chain Monorepo Environment Configuration
# NOTE: No environment variables are required to run or deploy the app.
# The project has built-in fallbacks for all services.

# App 1: Web Canvas App (apps/web)
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_STUN_SERVER=stun:stun.l.google.com:19302

# App 2: Docs App (apps/docs)
PORT=3001
